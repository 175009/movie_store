{% extends "::base.html.twig" %}

{% block title %}{{ movie.name }} - dodaj recenzję{% endblock %}

{% block body %}


  <div class="page-header">
    <h1>{{ block('title') }}</h1>
  </div>
  
  <div>
    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    
  </div>

{% endblock %}